# Utilizar o sistema?
1 - Primeiro clone o sistema 
2 - no terminal, vá até a pasta 
3 - dê um `npm install`, isso vai instalar todas as dependências necessárias
4 - dê apenas um `npm run dev`
5 - acesse a localhost sugerida, e experimente!
